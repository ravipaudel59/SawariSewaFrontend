<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sawariapatkalinsewa">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SawariApatkalinSewa"
        android:usesCleartextTraffic="true">
        <activity android:name=".Mechanicui.ViewRequestActivity"></activity>
        <activity android:name=".Customerui.AddRequestActivity" />
        <activity android:name=".Customerui.VehicleActivity" />
        <activity android:name=".Mechanicui.ViewFeedBackActivity" />
        <activity android:name=".Customerui.FeedbackActivity" />
        <activity android:name=".Mechanicui.UpdateMapActivity" />
        <activity android:name=".Mechanicui.UpdateBusinessActivity" />
        <activity android:name=".Mechanicui.ViewBusinessActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Mechanicui.MechanicMapActivity"
            android:label="@string/title_activity_mechanic_map" />
        <activity android:name=".Mechanicui.AddBusinessActivity" />
        <activity android:name=".Mechanicui.MechDashboardActivity" />
        <activity android:name=".Customerui.ViewMechanicActivity" />
        <activity
            android:name=".Customerui.DashBoardActivity"
            android:label="@string/title_activity_dash_board"
            android:theme="@style/Theme.SawariApatkalinSewa.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IntroductaryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>